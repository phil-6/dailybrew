<% @brews.each do |brew| %>
  <tr onclick="location.href='<%= brew_path(brew) %>'" class="brew-row row">
    <td class="coffee-name">
      <h5><%= brew.coffee.name %></h5>
    </td>
    <td class="equipment">
      <p><%= brew.equipment %></p>
    </td>
    <td class="config row">
      <span class="row"><i class="icon-basic-coffee-beans-1"></i><%= brew.coffee_weight.present? ? "#{brew.coffee_weight}g" : '-' %></span>
      <span class="row"><i class="icon-basic-water-drop"></i><%= brew.water_weight.present? ? "#{brew.water_weight}g" : '-' %></span>
      <span class="row"><i class="icon-basic-temperature"></i><%= brew.temperature ||= '-' %></span>
      <span class="row"><i class="icon-basic-timer"></i><%= brew.time != 0 ? brew.formatted_time : '-' %></span>
      <span class="row"><i class="icon-basic-favourite"></i><%= brew.rating ||= '-' %></span>
    </td>
    <td class="created">
      <p><small><%= brew.created_at > 1.week.ago ? "#{time_ago_in_words(brew.created_at)} ago" : brew.created_at.strftime("%d %b %Y") %></small></p>
    </td>
  </tr>
<% end %>
