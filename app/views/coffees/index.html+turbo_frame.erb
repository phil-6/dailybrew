<%= turbo_frame_tag "page_handler" do %>
  <%= turbo_stream_action_tag(
        "append",
        target: "coffees",
        template: %(#{render @coffees})
      ) %>
  <%= turbo_stream_action_tag(
        "replace",
        target: "coffees_pager",
        template: %(#{render "shared/index_pager", pagy: @pagy})
      ) %>
<% end %>
